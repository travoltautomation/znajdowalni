(()=>{const form=document.querySelector('#contact-form');if(!form)return;const startedAt=Date.now();let busy=false;form.addEventListener('submit',async e=>{e.preventDefault();if(busy||!form.reportValidity())return;const status=form.querySelector('[role=status]'),button=form.querySelector('button');busy=true;button.disabled=true;button.textContent='Wysyłamy…';try{const response=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...Object.fromEntries(new FormData(form)),startedAt}),signal:AbortSignal.timeout(20000)});if(!response.ok)throw new Error();form.innerHTML='<p role="status" tabindex="-1">Dziękujemy. Wiadomość została wysłana. Odpowiemy na podany e-mail.</p>';form.querySelector('[role=status]').focus();}catch{status.textContent='Nie udało się potwierdzić wysłania. Spróbuj ponownie lub skorzystaj z danych kontaktowych obok.';status.focus();button.disabled=false;button.textContent='Wyślij ponownie';}finally{busy=false;}});})();
